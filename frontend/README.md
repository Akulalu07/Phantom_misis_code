# Hack & Challenge Frontend - Фантомный МИСИС

Код веб-приложения для анализа отзывов.

## 🛠 Технологии

- **React 19** — библиотека для построения пользовательского интерфейса
- **TypeScript** — типизированный JavaScript
- **Vite 7** — сборщик и dev-сервер
- **TanStack Router** — файловая маршрутизация
- **TanStack Query** — управление серверным состоянием
- **HeroUI** — UI-компоненты
- **Tailwind CSS 4** — утилитарные CSS-стили
- **Vitest** — тестирование
- **pnpm** — менеджер пакетов

## 📁 Структура проекта

```
src/
├── features/                 # Бизнес-логика (Feature-Sliced Design)
│   ├── analyses/             # Работа с анализами
│   │   ├── api.ts            # API-запросы
│   │   ├── queries.ts        # TanStack Query хуки
│   │   ├── types.ts          # TypeScript типы
│   │   └── mocks.ts          # Моки для тестов
│   ├── clusters/             # Работа с кластерами
│   └── reviews/              # Работа с отзывами
│
├── routes/                   # Маршруты приложения (файловая маршрутизация)
│   ├── __root.tsx            # Корневой layout
│   ├── index.tsx             # Главная страница (/)
│   └── analyses/
│       └── $analysisId/      # Динамический маршрут анализа
│           ├── route.tsx     # Layout страницы анализа
│           ├── index.tsx     # Обзор анализа
│           ├── clusters.tsx  # Страница кластеров
│           └── reviews.tsx   # Страница отзывов
│
├── shared/                   # Общие утилиты и конфигурация
│   ├── config/
│   │   └── env.ts            # Переменные окружения
│   ├── lib/
│   │   ├── tanstack-query/   # Настройка TanStack Query
│   │   └── utils/            # Вспомогательные функции
│   └── styles/
│       └── globals.css       # Глобальные стили
│
├── widgets/                  # Композитные UI-компоненты
│   ├── analyses/
│   │   ├── create/           # Создание анализа
│   │   ├── list/             # Список анализов
│   │   └── overview/         # Обзор анализа
│   ├── clusters/
│   │   └── list/             # Список кластеров
│   ├── layout/
│   │   └── header/           # Шапка сайта
│   └── reviews/
│       ├── clusters/         # Кластеры в отзывах
│       ├── list/             # Список отзывов
│       └── statistics/       # Статистика отзывов
│
├── main.tsx                  # Точка входа
└── routeTree.gen.ts          # Автогенерируемое дерево маршрутов
```

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- pnpm 8+

### Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd hack-n-challenge-front

# Установить зависимости
pnpm install
```

### Настройка окружения

Создайте файл `.env` в корне проекта:

```env
# URL бэкенд API (по умолчанию: http://localhost:8000)
VITE_API_URL=http://localhost:8000

# Ссылка на GitHub репозиторий (опционально)
VITE_GITHUB_URL=https://github.com/Phantom-misis/frontend

# Версия приложения (опционально)
VITE_APP_VERSION=1.0.0
```

### Запуск

```bash
# Режим разработки (порт 3000)
pnpm dev

# Сборка для продакшена
pnpm build

# Предпросмотр продакшен-сборки
pnpm serve
```

## 📜 Доступные скрипты

| Команда       | Описание                                      |
|---------------|-----------------------------------------------|
| `pnpm dev`    | Запуск dev-сервера на порту 3000              |
| `pnpm build`  | Сборка для продакшена                         |
| `pnpm serve`  | Предпросмотр продакшен-сборки                 |
| `pnpm test`   | Запуск тестов (Vitest)                        |
| `pnpm lint`   | Проверка кода (ESLint)                        |
| `pnpm format` | Форматирование кода (Prettier)                |
| `pnpm check`  | Форматирование + исправление ошибок линтера   |

## 🧪 Тестирование

Проект использует [Vitest](https://vitest.dev/) для тестирования.

```bash
# Запуск всех тестов
pnpm test

# Запуск тестов в режиме наблюдения
pnpm vitest
```

## 🎨 Стилизация

Проект использует [Tailwind CSS 4](https://tailwindcss.com/) для стилизации и [HeroUI](https://heroui.com/) для UI-компонентов.

Глобальные стили находятся в `src/shared/styles/globals.css`.

## 📍 Маршрутизация

Приложение использует файловую маршрутизацию [TanStack Router](https://tanstack.com/router).

### Основные маршруты

| Путь                           | Описание                  |
|--------------------------------|---------------------------|
| `/`                            | Главная — список анализов |
| `/analyses/:analysisId`        | Обзор анализа             |
| `/analyses/:analysisId/clusters` | Кластеры анализа        |
| `/analyses/:analysisId/reviews`  | Список отзывов          |

## 🔧 Конфигурация

- **Vite** — `vite.config.ts`
- **TypeScript** — `tsconfig.json`
- **ESLint** — `eslint.config.js`
- **Prettier** — `prettier.config.js`
- **Tailwind** — настройки в `src/shared/styles/globals.css`

## 📦 Основные зависимости

| Пакет                          | Назначение                          |
|--------------------------------|-------------------------------------|
| `@tanstack/react-router`       | Маршрутизация                       |
| `@tanstack/react-query`        | Управление серверным состоянием     |
| `@heroui/react`                | UI-компоненты                       |
| `tailwindcss`                  | CSS-фреймворк                       |
| `lucide-react`                 | Иконки                              |
| `echarts-for-react`            | Графики и диаграммы                 |
| `framer-motion`                | Анимации                            |
| `react-dropzone`               | Drag & Drop загрузка файлов         |

## 🌐 Интеграция с бэкендом

Приложение взаимодействует с REST API бэкенда. URL настраивается через переменную окружения `VITE_API_URL`.

### Основные сущности

- **Analysis** — анализ файла с отзывами (статус, статистика)
- **Cluster** — кластер отзывов (заголовок, описание)
- **Review** — отдельный отзыв (текст, тональность, кластер)

### Тональность отзывов

- `positive` — положительный
- `neutral` — нейтральный
- `negative` — негативный
